iname: deploy app

on:
  push: 
    branches:
     - main

jobs:
  deploy:
     runs-on: ubuntu-latest
     
     steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: print message
        run: echo "Hello"

      - name: show files
        run: ls -la
        
